
*~
.*.sw*
*.bak

__pycache__
*.egg-info

src/build/
*.pyc
test/**/tmp
**/tmp/**
*.vscode
*.elf
*.elf.bin
!demo/**/*.elf
*.map

# p2im-drone firmware: not shipped in git; regenerated by build_drone.sh
test/firmware-rehosting/p2im-drone/Drone.elf
test/firmware-rehosting/p2im-drone/Drone.bin
test/firmware-rehosting/p2im-drone/drone_addrs.yaml
test/firmware-rehosting/p2im-drone/build/

# cnc-grbl firmware: not shipped in git; regenerated by extract.sh from
# the P2IM real-firmware dataset's prebuilt ELF.
test/firmware-rehosting/cnc-grbl/CNC.elf
test/firmware-rehosting/cnc-grbl/CNC.bin
test/firmware-rehosting/cnc-grbl/cnc_addrs.yaml
test/firmware-rehosting/cnc-grbl/build/
test/firmware-rehosting/cnc-grbl/hal_out.txt
test/firmware-rehosting/cnc-grbl/tmp/
deps/build-qemu/*.vsix
# Test output artifacts
hal_out.txt
test_out.txt
Receiver.txt
Sender.txt
test/zephyr/zephyr_fs/storage/
test/zephyr/zephyr_fs/vfs/

# Python / coverage
.coverage
.coverage.*
htmlcov/
.pytest_cache/

# Local virtualenvs
virtualenvs/
venv/
.venv/
.venv-mcp/

# Local MCP client wiring (machine-specific absolute paths) + session scratch
.mcp.json
tmp/mcp_session/

# VSCode extension session artifacts dropped into example/demo dirs
# (project config, Ghidra project, disassembly listing, handler cache)
halucinator.json
*.halucinator.json
.hal_bp_handlers.json
.ghidra_project/
*.gview
*.sqlite
*.sqlite-journal

# Generated SBOM (produced by CI, not checked in)
*.spdx.json
deps/build-qemu/

# run_backend_matrix.sh per-cell logs and run summary
.matrix_logs/
matrix_run.log

# `build_qemu.sh --upstream-qemu <ref>` clones vanilla upstream QEMU
# into deps/qemu-upstream-<ref>/ and applies tools/avatar-qemu-overlay
# before building into deps/build-qemu-upstream-<ref>/. Both the
# working clone and the build tree are ephemeral and per-ref so
# parallel builds for different upstream tags don't collide.
deps/qemu-upstream*/
deps/build-qemu-upstream*/
deps/build-qemu-libafl/

# Per-arch local QEMU builds (mounted into the matrix harness Docker
# container; not shipped). One directory per `-target-list` softmmu.
.build-arm/
.build-aarch64/
.build-mips/
.build-ppc/
.build-ppc64/

# Local CI replay scratch (`ci-replay.sh` plus its captured logs).
ci-replay.sh
ci-pytest.log
ci-replay.log
config-temp/
config.log
run_renode.sh

# bpv5 demo run artifacts (regenerated by the run_*.bash tests)
bpv5_*.log
bpv5_*_screen.png
bpv5_lcd_screen.png
test/falcon_fecs/*.bin
